# CanaryTrace dependencies, pinned to the environment the paper's results were
# produced with. Python 3.9.
#
# Install PyTorch matching your CUDA build FIRST -- it is deliberately not pinned
# here because the right wheel depends on your GPU:
#
#   CUDA 11.8 (A100 / H100, compute capability <= sm_90):
#     pip install torch==2.0.1 torchvision==0.15.2 torchaudio==2.0.2 \
#         --index-url https://download.pytorch.org/whl/cu118
#
#   CUDA 12.8 (B200 / Blackwell, sm_100):
#     pip install torch==2.7.1 --index-url https://download.pytorch.org/whl/cu128
#
# then:
#   pip install -r requirements.txt
#
# Both torch versions were validated against the pinned stack below.

# --- core pipeline ---
transformers==4.45.0
tokenizers==0.20.3
safetensors==0.4.2
accelerate==0.28.0
huggingface-hub==0.25.0
numpy==1.26.4
pandas==2.1.0
scipy==1.10.1
scikit-learn==1.3.1
tqdm==4.67.1
pyyaml==6.0.1
nltk==3.9.1
sentencepiece==0.1.99

# transformers 4.45 needs this explicitly on Python 3.9 with torch >= 2.7
importlib_metadata

# --- retrieval (BEIR + dense retrievers) ---
beir==2.0.0
sentence-transformers==2.2.2
faiss-cpu==1.9.0
datasets==2.21.0

# --- canary synthesis (OpenAI structured outputs) ---
openai==1.58.1
pydantic==2.10.4
